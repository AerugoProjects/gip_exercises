#include <stdio.h>

int main()
{
    int n;
    printf("Geben Sie eine positive ganze Zahl ein: "; // die Klammer muss noch geschlossen werden
    scanf("%d", &n);
    printf("Die Fakultaet von %d ist: ", n);
    f = 1;         // f wurde nicht initialisiert
    while (n > 0   // die Klammer muss noch geschlossen werden
        {
        f = f * m; // m wurde nicht initialisert und nicht definiert
        n = n - 1; //
        }
    printf("%d\n", f);
    return 0       // Semikolon am Zeilenende fehlt
}